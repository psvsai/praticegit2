Cloning one existing repo branch to new repo by using command "git clone -b branch-name and the repo ".
after cloning the branch . change the directory to the present file.
use git init command to reintilaized the repo or else error may occur if u use the new repo.
